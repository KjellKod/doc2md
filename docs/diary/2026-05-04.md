# Diary: 2026-05-04

## Quest: mac-license-notice-surface_2026-05-04__1005

Outcome: completed in worktree branch `quest/mac-license-notice-surface` (off `main`). Plan x2, fix x1, zero remaining findings.

User preferences observed:
- Use a worktree branch off main for new quest work; keep the active branch undisturbed.
- "Approved to build" after the executive summary, no walkthrough requested.
- Celebrate at end (option 1).

Corrections received:
- None mid-flight. The path-math defect was caught by Code Reviewer B (Codex), not by user intervention.

Decisions made:
- Plan: phased (5 phases: bundle resources, Help menu wiring, generator + npm wiring, drift test, README).
- Generator is a pure function of `package-lock.json` + `node_modules/<pkg>/package.json` (post `npm ci`) + `Package.resolved` + `scripts/notice-license-overrides.json`. No `xcodebuild`, no `.build/checkouts`. Marker-delimited regions only.
- Drift test (Vitest) spawns the generator in `--check` mode, captures stdout/stderr, asserts the verbatim remediation line. Placed under `src/__tests__/` (Vitest convention) rather than `tests/unit/` (Python-only here) — builder rationale accepted in review.
- Help menu wiring is additive (`CommandGroup(after: .help)`), uses U+2026 ellipsis, opens via `Bundle.main.url(...)` + `NSWorkspace.shared.open(_:)` from `MenuController`. No `webView` coupling. `ALLOWED_NATIVE_API_PATTERN` regex unchanged; only the human-readable `NATIVE_API_ALLOWLIST` got a docs entry.
- pbxproj patch: 4-section deterministic insert with pre-selected UUIDs (`9A0100…0012`/`0013`, `9A0200…0019`/`001A`).

Defects caught and fixed in-loop:
- `apps/macos/doc2md.xcodeproj/project.pbxproj`: desktop license `PBXFileReference.path` was `../../../LICENSES/...` (three `..`). The plan and arbiter both insisted three was correct; from `apps/macos/doc2md/Resources/`, three `..` lands at `apps/`, not at the repo root. Reviewer B (Codex) caught it. Fixer corrected to `../../../../LICENSES/...` (four `..`).
- pbxproj UUID-to-resource mapping was swapped vs the plan; restored for plan-fidelity.
- `src/__tests__/noticeInventoryDrift.test.ts` was missing `// @vitest-environment node`; added.

Validation:
- `npm run generate:notices` runs idempotent (byte-identical second run).
- `npm run generate:notices:check` exits 0.
- `npm test -- --run` passes including the new drift test.
- `git diff --check main...HEAD` clean.
- Manual macOS smoke (`npm run build:mac && open .build/mac/Build/Products/Release/doc2md.app`) is still required and remains the user's call. Auto mode does not run that.

Cross-agent conversation:
- Completion conversation succeeded. Dexter response logged at `.quest/mac-license-notice-surface_2026-05-04__1005/logs/conversation.log`. Theme: reviewers validate intent not arithmetic; next time mechanically resolve paths in build metadata. Memoir entry at `docs/dexter-journal/040-mac-license-notice-surface.md`.

Open questions:
- Should the generator also include a `--update-overrides` ergonomic mode for new SwiftPM dependencies, or is the current "edit `notice-license-overrides.json` by hand" path the right friction level?
- Worth adding a CI smoke that asserts `apps/macos/doc2md/Resources/../../../../LICENSES/LicenseRef-doc2md-Desktop.txt` (resolved) exists before bundle, to guard against future relative-path drift?

Next step:
- Manual macOS smoke per `feedback_mac_manual_validation.md`: `npm run build:mac && open .build/mac/Build/Products/Release/doc2md.app`. Verify Help → Acknowledgments…, Help → License…, About → Credits.
- If smoke passes, draft PR via the pr-assistant skill.

Narrative hook: Reviewer B counted the dots when nobody else did.
