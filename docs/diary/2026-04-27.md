# 2026-04-27

## Quest: mac-app-icon_2026-04-26__2355

Outcome: complete. Archived at `.quest/archive/mac-app-icon_2026-04-26__2355/`.

Implemented Mac Phase 6a app icon on `quest/mac-app-icon`: native `Assets.xcassets/AppIcon.appiconset`, 1024px source PNG, `sips` regeneration helper, Xcode target wiring via `ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon`, README maintenance instructions, and roadmap progress note.

Validation:

- `npm run build:desktop` passed.
- `bash scripts/build-mac-app.sh --configuration Release` passed.
- `scripts/verify-mac-release-launch.sh` passed after host-permission rerun; the sandboxed attempt failed on Xcode/SwiftPM cache writes outside the workspace.
- Built bundle contains `AppIcon.icns`, `Assets.car`, and `Info.plist` resolves icon name/file to `AppIcon`.
- Expanded secret/private-key scan was clean.

Review:

- Plan approved in one iteration.
- Dual code review passed with no fixer.
- Reviewer A left two optional doc clarity nits; Reviewer B found no issues.

User preferences and corrections:

- User explicitly asked to create the feature branch despite existing local docs edits, then asked to commit those existing edits onto the branch before Quest continued.
- User approved skipping the detailed plan walkthrough.
- User requested archive first, then celebration, then PR ready for review.

Decisions made:

- Archived raw quest artifacts before celebration per user request.
- No PR existed when celebration ran, so the celebration protocol fell back to journal number `029` and routed to Dexter's requiem style.
- Saved the requiem to `docs/dexter-journal/029-requiem-mac-app-icon.md` and indexed it.

Open questions:

- Maintainer should visually approve the app icon in Finder/Dock before merge, or replace the 1024px source PNG and rerun `bash scripts/generate-mac-icons.sh`.

## Quest: save-control-ui_2026-04-27__1212

Outcome: complete. Implemented on `quest/save-control-ui`.

Implemented Mac Phase 6c visible Save control and save-state UI:

- Added compact Save button and save-state pill near the preview/editor controls.
- Preserved Mac native Save semantics by routing visible Save through the same `handleSave` path used by Cmd+S.
- Preserved hosted web semantics by routing visible Save through the existing download/export path.
- Added hosted save-state tracking for `Saved`, `Edited`, and `Saving` while keeping Mac-only conflict/error semantics gated.
- Fixed the user-reported app-build bug where "Browse from your device" did not work by adding WKWebView open-panel support in `WebShellView.swift`.
- Updated the Phase 6a roadmap status body for PR #86.

Validation:

- `npm test -- --run` passed after the fixer pass: 43 files / 361 tests.
- `npm run build` passed after the fixer pass.
- `npm run build:desktop` passed after the fixer pass.
- `bash scripts/build-mac-app.sh --configuration Release` passed after the fixer pass.
- `scripts/verify-mac-release-launch.sh` was retried and blocked because an existing `doc2md` process was already running; the script refused to kill a session it did not launch.
- `python3 scripts/security_ci_guard.py` was not run because workflows and scripts were not touched.

Review:

- Plan approved after 2 iterations. Iteration 2 added the Browse-from-device app-build bug before Build.
- Initial dual code review found a hosted Save semantics regression around empty scratch drafts.
- Fixer tightened hosted Save to `isDownloadableEntry` for both handler and disabled state, added a regression test, and re-review passed cleanly.

User preferences and corrections:

- User declined the detailed plan walkthrough.
- User added a bug before Build and expected it to be folded into the same Quest rather than handled separately.
- User approved implementation explicitly after revised plan approval.

Decisions made:

- Treated the bug as pre-Build plan feedback, not an implementation ad hoc edit.
- Kept the app-build Browse fix in Swift WKWebView open-panel plumbing, not React import or conversion behavior.
- Did not force launch verification by killing the existing `doc2md` app process.

Open questions:

- Manual UI verification remains useful after quitting any running `doc2md`: hosted Save click/download, Mac Save button, Mac Browse-from-device file picker, and mobile toolbar at 375px/720px.
