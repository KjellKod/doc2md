# 2026-05-02

## Quest: dual-licensing-boundary_2026-05-01__2036

Outcome: complete. Landed mixed-license documentation and metadata for doc2md.

- User asked to turn the license research documents into a repo-aligned quest prompt, then approved running the quest on the existing `license-improvements-dual-model` worktree branch.
- Claude bridge preflight failed because the Claude CLI reported a usage limit, so the user explicitly chose option 2: continue as a Codex-only quest.
- The plan needed one refinement: both plan reviewers caught that the license-option matrix omitted per-option user trust impact. The planner added that dimension and the arbiter approved iteration 2.
- Implementation kept `@doc2md/core`, shared converters, and hosted web code MIT while moving desktop-specific UI and bridge code under the desktop shareware boundary.
- The Mac app boundary is now documented as source-visible shareware under `LicenseRef-doc2md-Desktop`, with human legal validation still required before paid publication.
- Validation passed: `npm run typecheck`, `npm run lint`, `npm run test:core`, `npm test`, and `npm run build`. The first `npm run test:core` failed before `npm ci` because the worktree lacked root `node_modules`; after installing dependencies it passed.
- `npm run build:mac` was skipped in the original Quest because that change only touched docs, license files, README, and package metadata.
- Cross-agent conversation was skipped because Claude was unavailable. Solo Dexter closeout was recorded in `docs/dexter-journal/036-dual-licensing-boundary.md`.

Open follow-ups:

- Have counsel review `LICENSES/LicenseRef-doc2md-Desktop.txt` before public paid reliance.
- Decide whether to tag/document the last repo-wide MIT release.
- Define contributor terms before accepting outside contributions to non-MIT desktop areas.
- Re-run an adversarial license review after each desktop-license boundary change.


## Quest: playwright-browser-baseline_2026-05-01__0755

Outcome: complete. Added a Chromium-only Playwright hosted-browser baseline, CI e2e job, failure-only artifacts, and post-review fixes for retry masking plus exact download event-count coverage.

User preferences and corrections:
- User chose full Quest mode, then approved Codex-only fallback for all designated roles after Claude quota was exhausted.
- During sharpen, user agreed to keep v0 constrained: no browser caching, real accessible selectors over `data-testid`, no product-only scratch draft naming, filename/event-only download assertions, coarse responsive checks, and helpers local to the single spec.

Decisions made:
- Kept the Quest gates even when switching Claude-designated roles to Codex fallback.
- Accepted the builder's follow-up note that sub-735px long-name overflow belongs in a separate responsive CSS quest, not this Playwright baseline.
- Removed Playwright CI retries after code review because v0 should fail on first deterministic browser failure.

Conversation and memory:
- Plan-stage Jean-Claude conversation succeeded and is logged in `.quest/playwright-browser-baseline_2026-05-01__0755/logs/conversation.log`.
- Completion cross-agent call was intentionally waived because the Claude bridge was quota-blocked; Dexter wrote the closeout memoir locally in `docs/dexter-journal/034-playwright-browser-baseline.md`.

Open follow-up:
- Consider a separate responsive CSS quest for uploaded long names below roughly 735px.

## Quest: phase-7-licensing-mvp_2026-04-30__2240

Outcome: complete. Implemented Mac-only commercial licensing MVP with offline public-key license verification, Keychain-first validity-aware local storage, Mac license UI, save-count reminders, Sparkle update-check preference behavior, hosted-web no-coupling guards, release/PR key-safety gates, docs, and final clean code review.

## User Preferences And Corrections

- User rejected Trial/Grace. Product model is free forever with reminders, paid licensed, or invalid/check-failed handling.
- User preferred save-count reminders instead of launch/day cadence: first after 10 successful saves, then every 25 successful saves in the current app session.
- User wanted disabled Mac-only `Buy License` affordance until purchase/key infrastructure is real, with no clickable external buy route yet.
- User approved frequent update checks by default, with licensed users able to toggle monthly checks.
- User explicitly asked to celebrate, then archive, commit, and push.

## Decisions Made

- Direct DMG and honest-user Mac licensing stay separate from hosted web and npm surfaces.
- `license.doc2md.dev` remains private issuer territory; the Mac app verifies local signed tokens offline.
- Release distribution builds fail closed on dev license public-key material.
- PR Release compile uses an explicit `--allow-development-license-key-for-pr` override so no-secrets PR CI can still build without weakening distribution defaults.
- Native file API scan now covers recursive Mac app Swift sources, including `apps/macos/doc2md/Licensing/`.

## Conversation And Runtime Notes

- Claude bridge became unavailable during the Codex-led quest because the Claude CLI reported a usage limit. Later plan review, arbiter, build, review, and fix roles used Codex fallback and logged runtime attribution in `.quest/phase-7-licensing-mvp_2026-04-30__2240/logs/context_health.log`.
- Completion cross-agent conversation was skipped for the same bridge-availability reason and recorded in the quest conversation log.

## Open Questions

- Production license public key and non-dev `key_id` still need real release setup before public paid distribution.
- Manual Mac app smoke remains useful before PR ready: license entry, invalid token, save-count reminder timing, disabled buy affordance, and update toggle behavior.
