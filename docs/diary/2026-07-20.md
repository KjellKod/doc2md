# 2026-07-20

## Quest: Mac license grace and expired-reminder states

### Context

Completed Quest `mac-license-grace-states_2026-07-16__1123` in the isolated worktree on `quest/mac-license-grace-states`. The user selected the full workflow, worktree mode, default orchestration, requested the plan walkthrough, asked for the concrete value before Build, and then explicitly approved implementation.

### Outcome

- Added pure cached-entitlement evaluation for `licensed`, `grace`, and `expiredReminder` from claims, key status, current time, and last validation time.
- Exact `expiresAt + 7 days` is `expiredReminder`; the instant immediately before remains `grace` when no definitive post-expiry answer exists.
- Added moment-of-use controller evaluation without timers or background jobs.
- Re-enabled the shipped reminder cadence in `expiredReminder` and exposed `licensedConveniencesPaused` for later licensed conveniences.
- Kept network/Polar code, `LicenseVerifier.swift`, Ed25519 behavior, shared desktop code, and all document operations untouched.
- Full Xcode tests, `npm run build:mac`, manual Release File → Open with `README.md`, and `git diff --check` passed.
- Both final canonical code reviews were clean; zero fix iterations.

### User Preferences And Decisions

- Preserve the complete Quest ceremony and explicit Build gate for commercial licensing work.
- Use an isolated worktree so `main` remains checked out.
- Explain the incremental product value before implementation rather than presenting foundation work as an immediate user-facing feature.
- Phase 1 is valuable as the deterministic entitlement foundation for Phase 2 revalidation and Phase 3 convenience gating; it intentionally adds no network client or paid convenience by itself.

### Review And Orchestration Notes

- Plan review caught an ambiguity at the exact seven-day grace endpoint. The Phase 1 plan was treated as authoritative: exact day seven ends grace. Iteration 2 pinned this in one truth table and explicit edge tests.
- Claude arbiter findings failed schema validation twice; the user authorized retries, including a Codex cross-runtime retry that produced valid canonical findings and allowed planning to continue.
- During code review, the findings-structure retry truncated Reviewer A's prose artifact. The workflow treated this as an infrastructure event, ran a full Codex cross-runtime fallback, and restored clean canonical review artifacts. This was not a product defect.
- Plan, code-review, and completion conversations with Jean-Claude succeeded and are recorded in `.quest/mac-license-grace-states_2026-07-16__1123/logs/conversation.log` before archival.

### Open Questions For The Next Session

- Phase 2 must keep network validation non-blocking and preserve the Phase 1 cached-expiry truth table rather than reinterpreting its boundaries.
- Retry-truncated review artifacts should remain suspect until independently recovered; consider hardening the runner so a structure-only retry cannot truncate the prose source it is meant to translate.
