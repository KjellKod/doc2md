# 2026-04-08

## Quest: ci-review-hardening_2026-04-08__1949

**Outcome:** Complete (Gold)

### Context
Kjell provided an idea doc (`ideas/ci-review-hardening-patterns.md`) describing 6 CI review hardening patterns. Routed as full workflow quest (medium risk, substantial complexity). The patterns covered trust zones, untrusted content delimiters, SHA pinning, trusted execution rules, output validation, and partial coverage surfacing.

### Human Inputs to Remember
- Kjell asked why the fix loop requires approval; answer was `auto_approve_phases.fix_loop: false` in allowlist
- Kjell asked to flip `fix_loop` to `true` in both doc2md and `~/ws/extra/quest` repos
- Kjell declined detailed plan walkthrough ("No"), prefers efficiency
- Kjell chose to celebrate

### Changes Made
- `.ai/allowlist.json`: `fix_loop` flipped to `true` (also in `~/ws/extra/quest`)
- `.github/codex-review-prompt.md`: Added `<untrusted_content>` delimiters
- `.github/workflows/codex-ci-review.yml`: SHA pinning, trust-zone comments, env-scoping, PR-head prompt fetch
- `scripts/codex_review_prepare.py`: `sanitize_untrusted()` on all 4 payloads, `parse_diff_ranges()` with LEFT+RIGHT
- `scripts/codex_review_post.py`: Side-aware diff-range validation, partial coverage surfacing, summary enhancements
- `tests/unit/test_codex_review_post.py`: Boundary tests, AC6 tests, deleted-line regression (55 total)
- `tests/unit/test_codex_review_prepare.py`: Sanitization, diff-range, rename, deleted-line range tests

### Decisions Made
- AC1: Defense-in-depth documentation, not real isolation (single-job architecture preserved)
- AC2: Commit messages excluded from scope (YAGNI)
- Sanitization covers all 4 interpolated payloads
- Diff-range validation is side-aware (LEFT for deletions, RIGHT for additions)

### Cross-Agent Conversation
Dexter held the requiem (darkly-amused mood). Saved to `docs/dexter-journal/017-requiem-ci-review-hardening.md`.

### Open Questions
- SHA pins will need periodic updates (Dependabot can handle this)
- Prompt template now fetched from PR head; worth monitoring whether this introduces new injection surface

### Next Step
Commit changes, create PR, run CI to validate the hardened workflow end-to-end.
