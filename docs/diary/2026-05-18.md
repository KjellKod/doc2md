# Diary: 2026-05-18

## AppShell Dedup, Phase 2 (Quest `appshell-dedup-phase-2_2026-05-16__1614`)

Context:
- Branch `refactor_v2_appshell_dedupe`, stayed in the parent repo workspace per user direction.
- Source proposal was `ideas/preview-panel-refactor.md`. Phase 1 (PreviewPanel mode split) shipped in PR #127. This quest is Phase 2: collapse `src/App.tsx` and `src/desktop/DesktopApp.tsx` into a shared `AppShell` with two platform adapters and a shared resize hook, preserving 100% of current behavior.
- Verified-on-disk current sizes before planning: `src/App.tsx` 1342 lines, `src/desktop/DesktopApp.tsx` 3262 lines. The original year-old proposal cited 756 and 2239. Duplication had grown 40 to 50 percent.

Human Inputs to Remember:
- The two authoritative briefs disagree on size ceilings. The user-typed `/quest` invocation said 200 / 300 / 700 with the word "target" and a parenthetical citing PR #127 soft ceilings. The on-disk verbatim brief at `ideas/quest-briefs/preview-panel-refactor-phase-2.md` said 50 / 80 / 400 plus useWorkspaceResize ≤ 200 and each adapter ≤ 400. Arbiter resolved as hard-vs-aspirational: hard merge-blocking on the looser numbers, aspirational targets on the stricter ones. User approved at the plan presentation gate without overriding the resolution.
- "Looks good, proceed to build" at the presentation gate was treated as covering both the Step 3.5 plan approval AND the Step 4 build-phase gate.
- User chose to retry the Codex fixer once after a stream-drop rather than fall back to Claude. The retry completed cleanly.

Changes Made:
- Quest folder `.quest/appshell-dedup-phase-2_2026-05-16__1614/` with full plan, dual reviews, arbiter verdict, builder notes, fixer feedback, and two clean code-review iterations.
- Four commits on `refactor_v2_appshell_dedupe`:
  - `d8f5b2d` characterization tests first (web Playwright spec + desktop vitest+installMockShell test, all five brief scenarios covered on both shells).
  - `f320cbf` `useWorkspaceResize` hook extracted.
  - `f6dabe7` `AppShell.tsx` + `webAdapter.tsx` + `desktopAdapter.tsx` extracted, shims rewired.
  - `e3fcc09` fixer pass: removed shim re-export of `computeEditShellCeiling`, converted `workingModeBarSlot` and `dropZoneSlot` from opaque ReactNode to typed `workingModeBarProps`/`dropZoneProps` rendered inside AppShell, expanded desktop characterization from 119 to 362 lines for full five-scenario coverage.
- Final sizes against the binding HARD ceilings:
  - `src/App.tsx` 66 lines (HARD 200 ✅, aspirational 50 missed by 16)
  - `src/desktop/DesktopApp.tsx` 65 lines (HARD 300 ✅, aspirational 80 ✅)
  - `src/shell/AppShell.tsx` 565 lines (HARD 700 ✅, aspirational 400 missed by 165)
  - `src/shell/useWorkspaceResize.ts` 653 lines (aspirational 200, accepted debt)
  - `src/shell/webAdapter.tsx` 456 lines (aspirational 400 missed by 56)
  - `src/shell/desktopAdapter.tsx` 2437 lines (aspirational 400, accepted debt, follow-up split note path: `ideas/notes-followup-desktopAdapter-split.md`)
- PreviewPanel internals untouched: `git diff main -- src/components/preview/ src/components/PreviewPanel.tsx` empty.
- Validation gates green: typecheck, lint (2 baseline warnings on desktopAdapter unchanged from pre-quest), 680 unit tests, 25 e2e tests, 12 new desktop characterization tests, `npm run build`, `npm run build:mac`, `git diff --check`.
- Companion bug `ideas/archive/bug_report_find_preview_table_cells.md` confirmed out-of-scope, no PreviewPanel behavior changed.
- Memoir entry: `docs/journal/044-celebrate-appshell-dedup-phase-2.md`; Dexter requiem at `docs/dexter-journal/047-requiem-appshell-dedup-phase-2.md` (next PR is #135, odd, so Dexter took the body).

Sources Consulted:
- `ideas/quest-briefs/preview-panel-refactor-phase-2.md` (full verbatim brief)
- `ideas/preview-panel-refactor.md` (original proposal)
- `docs/journal/041-celebrate-preview-panel-refactor-phase-1.md` (test-first pattern reference)
- `playwright.config.ts`, `vite.config.ts` (for the harness decision)
- `src/__tests__/App.desktop.test.tsx`, `src/desktop/mockShellBridge.ts` (existing desktop test infrastructure)
- Current source: `src/App.tsx`, `src/desktop/DesktopApp.tsx`, and the new `src/shell/**` modules

Security Notes:
- No changes to converters, save state machines, Sparkle, licensing, theme persistence semantics, or Mac native bridge contracts.
- No new dependencies. Four-file `src/shell/` surface held: `AppShell.tsx`, `useWorkspaceResize.ts`, `webAdapter.tsx`, `desktopAdapter.tsx`. No fifth file added.

Open Questions:
- The `desktopAdapter.tsx` at 2437 lines is internally coherent but six times over the aspirational target. The cleanest next slice is purpose-specific desktop hooks (useDesktopPersistence, useDesktopSave, useDesktopReload, useDesktopConflict, useDesktopSettings) which would require revisiting the four-file rule under `src/shell/`. Tracked at `ideas/notes-followup-desktopAdapter-split.md` (path noted, file not yet created).
- The desktop characterization test uses jsdom computed-style equivalence rather than real-browser pixel geometry, an explicit downgrade documented in commit messages and PR body. Future quest could promote it to a real Playwright desktop project once a `vite --mode desktop` server config is wired up.

Next Step:
- Open PR #135 from `refactor_v2_appshell_dedupe` via the `pr-assistant` skill, using `.quest/appshell-dedup-phase-2_2026-05-16__1614/phase_02_implementation/pr_description.md` as the description source.
- Archive the quest folder to `.quest/archive/` after the PR is open.

Narrative Hook:
- The shells finally stopped mirroring each other badly. The duplication that grew 40 to 50 percent per year is bounded by typed slot props and a single resize hook. The desktop adapter is still fat, but that is a known follow-up rather than ongoing rot.
