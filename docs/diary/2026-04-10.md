# 2026-04-10

## Quest: copy-mode-clipboard_2026-04-10__0010

**Outcome:** Complete (Diamond)

### Context
Kjell reported that the copy icon in preview mode copies raw markdown instead of formatted text. Solo quest on a worktree branch based off main. Two files changed, zero iterations.

### Human Inputs to Remember
- Kjell wants worktree branches based off main (explicitly overrode the startup script's skip behavior)
- Kjell tested the fix live: pasted into Google Docs, confirmed formatting preserved
- Kjell discovered a second issue: pasting LinkedIn unicode text into the editor preserves mathematical bold/italic chars instead of converting to markdown. Created idea doc for it.
- Kjell wants edit mode paste to either strip formatting or convert to markdown (ideally the latter)

### Changes Made
- `src/components/PreviewPanel.tsx`: Added `useRef` for preview container, `copyRenderedContent()` helper using `ClipboardItem` with `text/html` + `text/plain`, mode dispatch in `handleCopy()`, updated aria-labels
- `src/components/PreviewPanel.test.tsx`: New tests for rich clipboard copy, fallback, aria-labels (28 tests total)
- `ideas/paste-to-markdown-in-editor.md`: Idea doc for unicode-to-markdown conversion on paste
- `ideas/README.md`: Added new idea to index

### Decisions Made
- Preview mode copy icon uses `navigator.clipboard.write()` with both MIME types
- Fallback degrades to `innerText` (rendered text), not raw markdown
- Edit and LinkedIn modes unchanged (already correct)
- Paste-to-markdown deferred to a future quest

### Open Questions
- PR body gate required a "Changes" section, fixed after initial PR creation
- Codex CI review is in WAITING state (environment approval needed)

### Next Step
PR shepherd: monitor CI, respond to review comments, fix issues, mark ready for review.
