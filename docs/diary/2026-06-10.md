# 2026-06-10

## Star on GitHub call to action

### Context
Kjell asked for a discreet but visible "Star on GitHub" element near the top of
the web app, and a more discreet version at the bottom for the desktop app. He
asked for UX analysis before any implementation.

### Human Inputs to Remember
- Wants the CTA visible without "breaking" the UX. Discreet and visible at once.
- Web: top placement. Desktop: more discreet, at the bottom.
- Confirmed via question: place it in the web hero-top AND the shared About
  footer (the footer is shared, so desktop gets the bottom placement for free).
- Confirmed: static label, NO live star count. Honors the local-first /
  privacy-friendly posture (zero github.com requests on load).

### Changes Made
- New `src/components/StarOnGithubButton.tsx`: static repo link styled as an
  outline pill, `compact` ("Star") and `full` ("Star on GitHub") variants,
  lucide `Star` icon, aria-label "Star doc2md on GitHub".
- `src/shell/webAdapter.tsx`: added the compact pill to `heroActionsSlot`,
  left of the ThemeToggle. Lives on the landing hero only, which is
  `aria-hidden` + `inert` in working mode, so it self-hides once editing.
- `src/components/AboutSection.tsx`: added the full pill as `.about-star`
  between the credits tagline and the sponsor line (shared web + desktop).
- `src/styles/global.css`: `.star-github-button` mirrors the theme-toggle
  outline pill using theme-aware tokens, so dark mode needs no override.
- Desktop hero intentionally untouched; desktop gets the CTA via the shared
  About footer per Kjell's "bottom" request.

### Sources Consulted
- `src/shell/AppShell.tsx` (hero-top / hero-actions / AboutSection mount).
- `src/shell/webAdapter.tsx`, `src/shell/desktopAdapter.tsx` (slot wiring).
- `src/components/ThemeToggle.tsx`, `global.css` (pill styling to match).

### Security Notes
- No network calls added. Static anchor to the public repo only. No live
  star-count fetch, preserving the no-telemetry / local-first promise.

### Verification
- typecheck clean, lint clean (only pre-existing warnings).
- Unit suite: all relevant tests pass. The one suite flake
  (`noticeInventoryDrift`, an mtime-equality test) reproduced under full-suite
  parallelism with AND without my changes, and passes in isolation. Not mine.
- Visual check via Playwright against the dev server: confirmed the pill in
  dark hero, light hero, and the About footer.

### Open Questions
- Whether to also surface the pill in desktop working-mode chrome (left out on
  purpose to keep the tool surface uncluttered).

### Next Step
- Awaiting Kjell's go-ahead to commit (harness rule: commit only when asked).

### Narrative Hook
A single quiet star, parked beside the night-mode switch, asking nothing and
phoning home to no one.

---

## Update (merged as PR #183)

### How the design landed
- Kjell first approved both placements (hero-top + About footer), then iterated:
  1. Removed the hero-top compact pill entirely (it competed with the theme
     toggle). Hero-top is back to brand + theme toggle.
  2. The standalone footer pill added dead vertical space, so the star moved
     onto the About header row, to the right of the "Read the story" toggle.
     Header now wraps gracefully on narrow widths.
- Simplified `StarOnGithubButton` to its single form once the `compact` variant
  was unused. Verified each step with Playwright crops.

### CI / flake notes (the real story of this PR)
- A markdown-only follow-up commit still reddened CI, which made the point
  unarguable: this repo's CI is flaky under load, not the diff. Three distinct
  flakes observed across runs, each green on re-run with identical code:
  1. `e2e` `hosted mobile edit/view dominance` (mobile-safari) — unwaited
     `boundingBox()` after a View click, `retries: 0`. WebKit was never
     validated when this assertion was written (see journal 053 / PR #176).
  2. `noticeInventoryDrift` — mtime-equality race.
  3. `App.desktop.test.tsx` "does not save an imported entry whose conversion
     failed" — `waitFor` timeout race.
- Captured the e2e one as a follow-up: `ideas/webkit-mobile-edit-view-flake-hardening.md`.
  The two unit flakes are noted but not yet documented; offered to fold them in.

### Human Inputs to Remember (added)
- Prefers the star CTA in ONE place (About header beside "Read the story"), not
  duplicated top and bottom. Discreet over prominent.
- Confirmed the "commit only when asked" rhythm: makes the change, reviews, then
  says "commit and push" explicitly.

### Final State
- Merged 2026-06-11 (merge commit c6ccac3). Branch deleted local + remote.
- Open follow-up: broaden the flake idea note into a CI-flake backlog covering
  all three tests (awaiting Kjell's call).
