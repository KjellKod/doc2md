# 2026-04-21

## Quest: mac-shell-scaffold_2026-04-20__2231

Outcome: complete.

The user resumed the Mac Shell Scaffold quest, declined the detailed walkthrough, approved implementation, asked for celebration, then redirected to PR creation. The quest added the Phase 1 Mac-only app shell: SwiftUI + `WKWebView`, desktop Vite mode, Debug dev-server loading, Release bundled asset loading, Xcode Run Script resource copy, and local docs.

User preferences observed:

- The app should feel like an app experience, not a browser tab.
- The user prefers skipping ceremony walkthroughs when the plan summary is enough.
- When the work is ready, the user wants the PR created promptly.

Decisions made:

- Debug skips the desktop bundle script and loads `http://localhost:5173`.
- Release-style builds run `npm run build:desktop` before Copy Bundle Resources.
- The generated `apps/macos/doc2md/Resources/Web/` bundle stays ignored except `.gitkeep`.
- Full Xcode validation used `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` because `xcode-select` points at Command Line Tools.
- The manifest validator was tightened so workspace-local Claude memory files do not need machine-specific entries in `.quest-manifest`.

Cross-agent conversation:

- Claude bridge was unavailable due a Claude CLI usage limit, so the quest ran Codex-only.
- A solo reflection was recorded in `.quest/mac-shell-scaffold_2026-04-20__2231/logs/conversation.log`.

Open questions for next session:

- Before merging, visually confirm the Debug missing-server overlay in the app window.
- Decide whether PR creation should include the Dexter requiem and quest closeout files if the user wants a lean implementation-only PR.

## Quest: mac-phase2-bridge_2026-04-21__0712

Outcome: complete.

Same day as Phase 1 but a distinct quest: the JavaScript bridge contract and the React desktop mode, no real filesystem writes. The user launched in auto mode with a fully scoped brief (goal, 10 required scope items, acceptance criteria, validation list, and explicit constraints). Ran as a full workflow quest with dual plan review + arbiter + dual code review + fixer. Claude (Opus 4.7) took planner, arbiter, and code-reviewer-a. Codex (gpt-5.4) took plan-reviewer-b, builder, code-reviewer-b, and fixer.

User preferences observed:

- When the brief is already detailed, auto mode is acceptable even through the plan approval and build gates; visibility first, interrupt if needed.
- `danger-full-access` remains the right sandbox for Codex MCP calls — the workspace-write attempt produced zero-byte artifacts even when Codex claimed success.

Decisions made:

- The bridge contract is the product. Plan iteration 2 committed five binding decisions: keep `ShellPermissionNeeded` in the shared contract (and update design doc section 4), narrow `revealInFinder` to `ShellRevealOk = { ok: true; path: string }`, keep `Doc2mdShell.version: 1` with an explicit handshake note, bridge promises resolve with result objects (rejections only for transport/programming faults), and Phase 2 methods are exactly `openFile/saveFile/saveFileAs/revealInFinder`.
- Close Window dispatches `doc2md:native-close-window` first, then Swift calls `NSApp.keyWindow?.performClose(nil)` in the `evaluateJavaScript` completion handler.
- Swift stub no-I/O guarantee is named as a forbidden-API list in the plan and the PR description checklist.

Cross-agent conversation:

- Dexter debriefed post-quest on likely Phase 3 break points: security-scoped URLs/bookmarks, path lifetime across relaunch, canceled panels, permission-denied mapping, save-state drifting from native menu events. His advice: keep Swift adapting to the v1 contract, never mutating it for convenience; typed failures everywhere; test moved files, denied permissions, canceled dialogs, reveal failures, and repeated menu listener cycles.
- Log: `.quest/mac-phase2-bridge_2026-04-21__0712/logs/conversation.log`.

Open questions for next session:

- Full Xcode is not selected on this host (Command Line Tools only), so `xcodebuild` Debug/Release and a manual Debug shell launch are deferred to merge. Worth noting in the PR description.
- Phase 3 scope: decide ordering of openFolder, watchFile, checkForUpdates — and whether watchFile makes sense before autosave is in place.
