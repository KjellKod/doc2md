# Diary: 2026-05-26

## Mobile real-device fix

Context:
- Branch `mobile-view`.
- Quest `mobile-real-device-fix_2026-05-23__0940` ran Codex-only by explicit user request.
- The user provided phone screenshots and asked whether the editor workflow should copy the Install & Use zoomed-out behavior or be truly mobile-friendly.

Human Inputs to Remember:
- Kjell wanted the Quest run with Codex only in every phase.
- Option `3` at the plan gate meant proceed to build.
- The practical concern was not cosmetic polish; the phone view looked horizontally panned even after the previous branch.

Changes Made:
- Added named Playwright mobile projects for Pixel 7 Chrome and iPhone 14 WebKit.
- Added hosted mobile e2e coverage for collapsed upload rail alignment, viewport bounds, keyboard-open editing, mode switching, error recovery, and loaded-document collapse behavior.
- Updated CI to install Chromium and WebKit and run the mobile layout spec against both mobile projects.
- Scoped editor mount autofocus so it only fires on explicit editor focus requests.

Decisions Made:
- Did not implement global zoom, root font shrink, or `transform: scale`.
- Classified the observed public-site symptom as stale deploy: live GitHub Pages assets lacked the current hosted selectors and current hero copy.
- Kept physical-phone validation as a post-deploy manual gate because Playwright mobile emulation did not reproduce the stale public screenshot on current source.

Conversation and Persona Notes:
- Jean-Claude bridge/persona conversation was intentionally waived to honor the user's Codex-only instruction.
- Waiver recorded in `.quest/mobile-real-device-fix_2026-05-23__0940/logs/conversation.log`.
- Dexter journal entry written at `docs/dexter-journal/048-requiem-mobile-real-device-fix.md`.

Validation:
- `npm run build`
- `npm run typecheck`
- `npm run lint` with existing `desktopAdapter.tsx` react-refresh warnings only.
- `npm test -- --run` passed 726 tests.
- `npm run test:e2e -- --project=chromium tests/e2e/hosted-mobile-tablet-layout.spec.ts`
- `npm run test:e2e -- --project=mobile-chrome --project=mobile-safari tests/e2e/hosted-mobile-tablet-layout.spec.ts`

Open Questions:
- After deployment, verify `https://kjellkod.github.io/doc2md/?cacheBust=<timestamp>` on the target phone/browser before declaring the public site fixed.

Next Step:
- Commit the implementation and Quest artifacts, then open/update the PR with the stale-deploy note and physical-phone validation checkbox.
