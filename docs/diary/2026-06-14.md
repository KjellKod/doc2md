# 2026-06-14

## Mobile reading-space quest (P0–P2) — PR #185

### Context
Kjell sent an iPhone screenshot: the hosted (web) mobile view spends most of the
glass on chrome, leaving a sliver for the actual document. He first asked for
research only ("don't make code changes, just research how this can be better"),
then agreed with the findings and kicked off a full `/quest` to implement them,
ending in a review-ready PR, celebration, archive, and pr-shepherd.

### Human Inputs to Remember
- Strong preference: maximize mobile **reading space**; chrome should be much
  smaller. Functionality over chrome.
- Decision on the contested item: **"defer P3, measure first."** P3 (let
  read-only View flow past Edit) conflicts with PR #176's deliberate Edit==View
  parity invariant; Kjell chose to ship P0–P2 (chrome reduction) and measure the
  reclaimed space before deciding whether P3's regression risk is worth it.
- Granted full autonomy for this quest: may commit, push, and create a PR without
  asking. (Distinct from his usual "commit only when asked" rhythm — this was an
  explicit per-quest waiver.)
- Wants the PR **ready for review**, not draft.

### Changes Made
- P0: `collapseSidebarOnPhoneSelect` in `useWorkspaceResize`, wired from a
  post-select effect in `webAdapter` (reads `isScratch` after re-derive; honors
  `userTouchedSidebarRef`). Tapping a file fills the phone screen.
- P1: new `PreviewOverflowMenu`; single-band toolbar via a web-only
  `compactToolbar` render-layer split (App → AppShell → PreviewPanel →
  PreviewToolbar). Save structurally lifted into the primary band.
- P2: rail folded into the working-mode bar via the existing `trailingControls`
  slot; rail hidden in working mode only (CSS-only, shared AppShell branch
  untouched).
- Fix iteration: the build briefly re-created the P0 dead-end (after a manual
  reopen, `userTouchedSidebarRef` stayed set and the only collapse control is
  `display:none` on phones). Resolved with a Show/Hide **toggle** in the
  working-mode bar that calls `handleCollapseSidebar` directly (not gated by the
  ref). Plus a compact-mode shortcuts focus-return fix.
- PR #176 Edit==View parity assertion and the iOS keyboard-occlusion fix left
  untouched and passing. P3 deferred with a concrete before/after measurement
  plan recorded in the plan.

### Sources Consulted
- `docs/journal/053-celebrate-mobile-edit-view-optimize.md` and the #176 keyboard
  fix (the load-bearing constraint).
- `tests/e2e/hosted-mobile-tablet-layout.spec.ts` (parity `:200`, collapse-rail
  `:304`, keyboard-open AC1/AC8).
- `docs/diary/2026-06-10.md` (Kjell's commit rhythm + WebKit/mobile-safari CI
  flake history).

### Security Notes
- No network calls, no secrets, no data-loss surfaces. Re-collapse is fully
  reversible. Nothing sensitive exposed.

### Open Questions
- WebKit/mobile-safari was not runnable in the container (browser not installed);
  it's the CI gate, same honest posture as #176. Needs a real-device / CI
  WebKit pass before merge.
- The deferred P3 measurement (preview-panel height + surface-dominance ratio at
  375×800, before vs after) should be run so Kjell can decide on P3.

### Next Step
- pr-shepherd watching PR #185 through CI + review; merge after WebKit CI is green
  and any review feedback is addressed.

### Narrative Hook
A quest to kill a dead-end briefly dug a new one — and the second reviewer, the
one some would call redundant, was the only chair in the room that noticed.

---

## Process note (Quest mechanics)
- Single-model run: Codex MCP unavailable in the ephemeral container, so all nine
  roles ran on Claude (remapped in `orchestration.json`). Same fallback as #176.
- Handoff.json compliance 14/14 (100%). Plan iterations: 2 (one was a scope
  reduction after Kjell deferred P3, not a quality failure). Fix iterations: 1.
- The arbiter twice needed a one-shot retry to satisfy the findings schema
  (`kind` enum; UX `principle_id`) — cheap, deterministic, worth noting as a
  recurring small friction in single-model arbiter output.
