# 2026-07-16

## Shepherding PR #195 through nine rounds of Codex review

### Context
Kjell invoked /pr-shepherd on PR #195 (the Polar commercialization
amendments). CI review (Dexter/Codex) turned out to be the best adversarial
reader the plan ever had.

### Human Inputs to Remember
- Kjell asked "nitpicking?" about a finding; the honest answer was half yes,
  half no, and the half-no mattered. Verdicts should stay honest even when a
  reviewer is a bot we could dismiss.
- Force-push needed Kjell to run it himself (`! git push --force-with-lease`)
  after the auto-mode classifier blocked me twice; his standing rule requires
  his own hands on that lever.

### Changes Made (commits 8ccf67e through c4be17d)
Nine review rounds, every finding accepted and fixed in the plan/spec docs:
1. Persist Polar's activation ID (storage, /validate requests, relaunch tests).
2. Model key status (granted/revoked/disabled); revoke honors expires_at then
   skips grace.
3. Fix contradictory Document Library gating wording; stale roadmap heading.
4. Mocked tests gate Phase 2 merge; sandbox e2e is deferred validation.
5. Replace deleted whats-next.md reference; due-diligence sign-off gates
   go-live, keeping Phases 1-3 human-prerequisite-free.
6. Polar key is a bearer credential: Keychain-only, fallback holds only
   non-secret metadata; Keychain loss degrades to re-entry.
7. Expired-but-granted entitlements keep retrying (throttled) so late
   renewals recover; cached expires_at is authoritative, validation never
   extends past it; activation-limit lockout gets a recovery path (Polar
   portal deactivation, support slot reset, limit headroom).
8. Request contract: public organization_id as build config (explicitly not
   a merchant credential), privacy-safe device label with persistent
   non-secret suffix so same-model Macs stay distinguishable.
9. License removal/replacement frees the activation slot via the public
   /deactivate endpoint, best-effort and non-blocking, so ordinary license
   changes never leak slots (this round landed after the first version of
   this entry; count corrected from eight to nine).

### Sources Consulted
- Codex inline findings on PR #195; Polar customer-portal API docs (earlier
  session); LicenseVerifier.swift and SessionStore.swift for grounding.

### Security Notes
- The bearer-credential storage split (Keychain vs metadata fallback) and the
  org-id/credential distinction both tightened the public-repo boundary.

### Open Questions
- App.desktop.test.tsx flaked three more times today (three re-runs, four
  distinct test names now). It needs a hardening quest; memory updated.

### Next Step
Kjell merges PR #195; post-merge cleanup, then the Phase 1 license state
machine quest.

### Narrative Hook
Dexter reviewed the plan eight times and was right eight times. The requiem
he owes this odd-numbered PR should really be a duet.
