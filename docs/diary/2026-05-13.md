# 2026-05-13 — Find-area bugs (quest find-bug-validation_2026-05-12__2339, PR #123 ux-bugfixes-May12)

## Context

Worktree branch `ux-bugfixes-May12` at `/Users/kjell/ws/extra/doc2md/.claude/worktrees/ux-bugfixes-May12`. The user asked for a reproduction-first audit of two find-area bugs documented in `ideas/bug_report_find_match_scrolls_to_wrong_line.md` and `ideas/bug_report_find_highlight_dom_leaks.md`. Hard gate: no production change before a failing test. Both bugs reproduced, both fixed, PR #123 merged 2026-05-13 (merge commit `b1721e9`).

## Human Inputs to Remember

- "HARD GATE — REPRODUCTION TEST FIRST. NO CODE CHANGES BEFORE A FAILING TEST." — honored throughout. Bug 2 reproduction needed three Playwright variants before the Enter-key path produced a real failure; the chevron-button + mode-switch variants were false negatives until the Enter-driven re-render path stressed the DOM-mutation race.
- "/quest" → solo run, then approved through Plan → Build → Code-review pipeline with full dual-reviewer + arbiter cycle for both Plan and Code-review phases.
- Manual walkthrough after merge surfaced two new issues: (a) the edit-mode find <mark> drifted above the matched word on macOS classic scrollbars; (b) the highlight moved relative to the matched text while scrolling. Both rooted in the same wrap-width divergence between `<textarea overflow:auto>` (reserves scrollbar gutter) and `<pre.markdown-find-overlay overflow:hidden>` (reserves none).
- Final review note: "the highlight for search match makes the text very hard to see" in dark mode. Tuned the dark-mode highlight (lower alpha + brighter outline on the edit overlay, forced dark glyph on the rendered <mark>).

## Changes Made

PR #123, six commits, all green CI:

- **Bug 2 fix (rendered DOM leak):** Deleted `applyRenderedFindHighlight`, `clearRenderedFindHighlight`, `findTextPosition`, `textNodesFor`, `removeEmptyRenderedInlineElements`, the `clearRenderedFindHighlight` call in `switchMode`, and the now-unused setActiveSelection branch in `useFindReplace.ts`. Added `src/components/findHighlightRehype.ts` (rehype plugin walking the hast tree by rendered-text offsets, wrapping matches in `<mark>`, handling cross-emphasis, zero-width regex matches with U+200B sentinel, and HTML entities). Added U+200B scrub in the `renderedViewText` snapshot effect so the sentinel doesn't shift offsets for subsequent queries (reviewer B finding).
- **Bug 1 fix (wrong-line scroll):** Deleted `scrollTextareaToMatch`. Extended `scrollTextareaToLine` with `offsetFraction` (0 = top, 0.5 = center, clamped). The find-active-match layout effect now calls `textarea.setSelectionRange(end, end)` then `scrollTextareaToLine(..., 0.5)`. New centering layout effect uses `element.querySelector("mark.markdown-rendered-find-highlight") + centerElementInScrollContainer` for preview/LinkedIn.
- **Cubic LinkedIn-mode regression:** The first iteration of the rehype-plugin fix only ran through `react-markdown`, so LinkedIn-mode lost active-match highlighting. Added `renderLinkedInSegmentChildren` helper that walks a cursor through `linkedinPreview`, intersects each segment with `[match.start, match.end)`, and wraps the matched portion in `<mark className="markdown-rendered-find-highlight">` inline. Playwright regression in `tests/e2e/find-linkedin-highlight.spec.ts`, load-bearing.
- **macOS classic-scrollbar wrap divergence:** Added `scrollbar-gutter: stable` to both `.markdown-edit-area` and `.markdown-find-overlay` so they reserve identical gutter regardless of OS scrollbar mode. Bug is not reproducible in headless Chromium (Playwright always uses overlay scrollbars even when targeting `::-webkit-scrollbar`), so the test in `tests/e2e/find-edit-overlay-wrap.spec.ts` pins the computed `scrollbarGutter` property on both elements; load-bearing on the CSS rule.
- **Dark-mode highlight legibility:** Edit overlay yellow dropped from 0.82 to 0.32 alpha + 0.85-alpha outline so dark-mode text underneath stays legible. Rendered `<mark>` (preview + LinkedIn) now forces `color: #1a1a1a` so light dark-mode text doesn't render light-on-yellow.

Tests: 593 unit, 7 find-related Playwright tests (find-highlight-leak symptom A/B + Enter variant, find-match-scroll, find-linkedin-highlight, find-edit-overlay-wrap × 2). All green on CI.

## Sources Consulted

- `ideas/bug_report_find_highlight_dom_leaks.md` (now archived) — Symptom A/B description + fix sketch.
- `ideas/bug_report_find_match_scrolls_to_wrong_line.md` (now archived) — soft-wrap scroll-math root cause.
- `src/components/viewportAnchor.ts` — existing measured-mirror helper to route through.
- `src/components/findHighlightRehype.ts` (new) — rehype walker.
- `src/components/PreviewPanel.tsx` — central plumbing for find + edit overlay.
- cubic-dev-ai[bot] inline comment at PreviewPanel.tsx:453 — LinkedIn-mode regression discovery.

## Security Notes

No new attack surface. The rehype plugin operates on hast (parsed/sanitized) data; it does not introduce dangerouslySetInnerHTML or string-level HTML injection.

## Open Questions

- The `scrollbar-gutter: stable` fix is verified via property-pin in CI, not visual regression. If macOS classic-scrollbar behavior regresses again, we need a manual-test entry in the PR template or a CI runner with classic scrollbars enabled.

## Next Step

Worktree and branch pruned post-merge per the auto-cleanup memory. PR #123 closed-merged. No follow-up tickets opened.

## Narrative Hook

Three bugs, one feature surface. Reproduction-first held the line — Bug 2 took three attempts to fail loudly, but once the Enter-key path landed, the DOM-mutation race was undeniable. The cubic bot caught the LinkedIn regression at the exact line where the rehype plugin was wired in; that's the kind of inline review that earns its keep. Dark-mode polish came at the end with a single user message and twenty lines of CSS.

— Jean-Claude

---

# 2026-05-13 (cont.) — PreviewPanel Refactor Phase 1 (quest preview-panel-refactor-phase-1_2026-05-13__2241)

## Context

Worktree branch `worktree-preview-panel-refactor` at `/Users/kjell/ws/extra/doc2md/.claude/worktrees/preview-panel-refactor`. The user asked for a structural refactor of `src/components/PreviewPanel.tsx` (1285 lines, up from the 1038 cited in the year-old proposal at `ideas/preview-panel-refactor.md`). Quest brief drafted, saved to `ideas/quest-briefs/preview-panel-refactor-phase-1.md`, then `/quest`-orchestrated with full pipeline (dual plan reviewers + arbiter + dual code reviewers + fix loop). The three known find/anchor bugs at this seam were intentionally NOT in scope.

## Human Inputs to Remember

- "Risk is high but value is high also." — confirmed during quest invocation. Honored by routing to full workflow (not solo) and preserving the test-first commit ordering even when it cost an extra characterization-test commit before any structural change.
- "We have 3 bugs also but this quest goal is not fixing the bugs — we shouldn't make things worse but the focus is refactoring." — the table-cell bug (`ideas/bug_report_find_preview_table_cells.md`) stays open with unchanged failure shape, verified post-refactor. The two closed bugs (archived) stayed closed.
- "Work without stopping for clarifying questions." — honored throughout the orchestration. The one mandatory human gate (Step 3.5 plan presentation, hard requirement per workflow.md) was the only pause; user selected option 3 (proceed to build).

## Changes Made

Eight commits on `worktree-preview-panel-refactor`, branched from `main` at `e4f4e43`:

1. `14c8e0f test: characterize preview panel mode behavior` — `tests/e2e/preview-panel-refactor-characterization.spec.ts` (194 lines) covering the four required cases plus two new unit specs `src/components/__tests__/useViewportAnchor.test.tsx` (128) and `src/components/__tests__/useFindHighlight.test.tsx` (67). Verified to pass against pre-refactor `main`.
2. `4c8ea40 refactor: add viewport anchor hook` — `src/components/preview/useViewportAnchor.ts` (79 lines) thin wrapper.
3. `1e26e5c refactor: add find highlight hook` — `src/components/preview/useFindHighlight.tsx` (9 lines) — short but earns its keep via `useMemo` stabilizing the rehype plugin reference.
4. `4d7b84f refactor: extract preview edit mode` — `src/components/preview/EditMode.tsx` (373) incl. `renderFindHighlight` move from old `PreviewPanel.tsx:260-286`.
5. `81294a1 refactor: extract preview rendered modes` — `src/components/preview/PreviewMode.tsx` (170), `LinkedInMode.tsx` (325), with snapshot dep-array narrowing per plan builder notes.
6. `5ae5518 refactor: split preview panel shell` — `src/components/preview/PreviewPanel.tsx` (350), plus flat extractions `PreviewToolbar.tsx` (167), `PreviewEmptyStates.tsx` (102), `previewCopy.ts` (57). Old shell-side combined anchor `useLayoutEffect` deleted as planned. `src/components/PreviewPanel.tsx` becomes a 2-line compat re-export.
7. `80f4ad1 refactor: clean preview anchor hook types` — post-React-19 ref-type cleanup (deliberate; defended in iter-2 review as out-of-scope to flatten).
8. `14731eb refactor: hoist viewportTopFloor above first use in shell` — fix-loop cra-001 from code reviewer A; one function declaration moved up ~30 lines, byte-identical body.

Plan iterations: 2 (iter 1 had two real gaps — shell line budget understated by ~200 lines and anchor handoff under-specified; iter 2 fixed both with a line-ownership table and `applyAnchorLine(line) === true` gating). Fix iterations: 1.

Forbidden-file invariant: `git diff main -- src/App.tsx src/desktop/DesktopApp.tsx` = empty. Import path preserved.

## Sources Consulted

- `ideas/preview-panel-refactor.md` — the year-old proposal that turned out to be correct.
- `ideas/bug_report_find_preview_table_cells.md` — open bug, used for post-refactor containment verification.
- `ideas/archive/bug_report_find_highlight_dom_leaks.md` + `bug_report_find_match_scrolls_to_wrong_line.md` — closed in PR #123, historical context.
- `src/components/PreviewPanel.tsx` (pre-refactor, 1285 lines) — read in full for planner line-ownership table.
- `src/components/viewportAnchor.ts`, `findHighlightRehype.ts`, `sourceLineRehype.ts`, `useFindReplace.ts`, `FindReplaceBar.tsx` — read by hooks during wrapping.
- `tests/e2e/view-anchor-mode-switch.spec.ts:359-411` — the LinkedIn refusal-interlude invariant that drove the `applyAnchorLine` boolean gating in the plan.

## Security Notes

No new attack surface. Refactor only moves code; no new dependencies, no `dangerouslySetInnerHTML`, no I/O surface change. The rehype plugin output stays byte-identical (verified by `useFindHighlight.test.tsx` and the unmodified `findHighlightRehype.test.tsx`).

## Open Questions

- **Baseline rot.** `npm run lint`, `npm run typecheck`, `npm test` fail on `main` for reasons entirely outside the refactor diff: `react-hooks/set-state-in-effect` rule referenced but not registered in `src/desktop/DesktopApp.tsx` (PR #120 fallout), `read-excel-file` v9 typing in `src/converters/office.ts` (`Row.sheet`/`Row.data` missing), plus some unrelated test-file drift. Builder correctly reported `STATUS: blocked` rather than papering over; reviewers verified failures reproduce on `main` and approved the refactor anyway. Recommend a separate **baseline rot quest** before merging this PR, OR a minimal "add baseline fixes inside this PR with a note" decision. Either path is defensible; the refactor itself is independently correct.
- **Mac manual validation pending.** Per the `feedback_mac_manual_validation.md` memory pattern: `npm run build:mac && open .build/mac/Build/Products/Release/doc2md.app`, then File → Open sample fixtures and exercise the four characterized seams. Not run yet in this session.

## Next Step

1. Decide on baseline rot: separate quest first, or include minimal fixes in this PR.
2. Manual Mac validation per memory pattern.
3. Open the PR (this is human-driven per the brief — builder did not push or open one).
4. After Phase 1 lands, the Phase 2 (`AppShell` dedup of `App.tsx` + `DesktopApp.tsx`) brief is the natural follow-up. The proposal is already written; only the brief needs sharpening.

## Narrative Hook

The proposal was a year old and grew more correct over time, not less. The refactor that was "not blocking" in 2025 turned into the thing that lets the next find/anchor fix land in 350 lines instead of 1285. Test-first held the line — characterization commit first, refactor commits second, fix commit last. Reviewer A's one-line sign-off on the fix commit ("the function packed its bag, walked 30 lines uphill, unpacked the same body") is the right energy for behavior-preserving surgery. Codex took the planner, builder, and fixer slots; Claude took both Reviewer A passes plus the arbiter. 14/14 structured handoff compliance. Two clean rounds, no contested findings.

— Jean-Claude
